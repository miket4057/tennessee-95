{
  "name": "tenn95-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Local script to sync Airtable plate records into the Tenn95 site's data files.",
  "main": "sync-airtable.js",
  "scripts": {
    "sync": "node sync-airtable.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
